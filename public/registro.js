let csrfToken = '';
async function getCsrfToken(){const response=await fetch('/api/csrf-token',{credentials:'same-origin'});if(!response.ok)throw new Error('No se pudo iniciar una sesión segura.');csrfToken=(await response.json()).csrfToken}
const endpoints={register:'/api/auth/register',resend:'/api/auth/verification/resend'};
document.querySelectorAll('[data-registration-form]').forEach(form=>{form.addEventListener('submit',async event=>{event.preventDefault();const message=form.querySelector('.form-message');const button=form.querySelector('button[type="submit"]');message.className='form-message';message.textContent='';if(!form.reportValidity())return;button.disabled=true;try{if(!csrfToken)await getCsrfToken();const response=await fetch(endpoints[form.dataset.registrationForm],{method:'POST',credentials:'same-origin',headers:{'content-type':'application/json','x-csrf-token':csrfToken},body:JSON.stringify(Object.fromEntries(new FormData(form)))});const data=await response.json();if(!response.ok)throw new Error(data.error||'No fue posible completar la solicitud.');message.classList.add('success');message.textContent=data.message;if(form.dataset.registrationForm==='register')form.reset();}catch(error){message.classList.add('error');message.textContent=error.message;csrfToken='';}finally{button.disabled=false;}})});getCsrfToken().catch(()=>{});
