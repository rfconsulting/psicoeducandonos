let csrfToken='';async function request(url,options={}){const response=await fetch(url,{credentials:'same-origin',...options,headers:{...(options.body?{'content-type':'application/json'}:{}),...(csrfToken?{'x-csrf-token':csrfToken}:{}),...options.headers}});if(response.status===401){location.replace('/login.html');throw new Error('Sesión finalizada.')}const data=await response.json();if(!response.ok)throw new Error(data.error||'Ocurrió un error.');return data;}
async function init(){const id=Number(new URLSearchParams(location.search).get('id'));const message=document.querySelector('#course-message');try{csrfToken=(await request('/api/csrf-token')).csrfToken;const {course,enrollment,modules}=await request(`/api/learning/courses/${id}/structure`);document.title=`${course.title} · Psicoeducándonos`;document.querySelector('#course-title').textContent=course.title;document.querySelector('#course-description').textContent=course.description;const list=document.querySelector('#module-list');modules.forEach(module=>{const section=document.createElement('section');section.className='course-module';const heading=document.createElement('h2');heading.textContent=module.title;section.appendChild(heading);module.lessons.forEach(lesson=>{const article=document.createElement('article');article.className='course-lesson';const title=document.createElement('h3');title.textContent=lesson.title;const content=document.createElement('p');content.textContent=lesson.content;article.append(title,content);if(enrollment){const button=document.createElement('button');button.className='small-button';button.textContent=lesson.completed?'Completada ✓':'Marcar como completada';button.disabled=lesson.completed;button.addEventListener('click',async()=>{try{await request(`/api/learning/lessons/${lesson.id}/progress`,{method:'PATCH',body:JSON.stringify({completed:true})});button.textContent='Completada ✓';button.disabled=true;}catch(error){message.className='form-message error';message.textContent=error.message;}});article.appendChild(button);}section.appendChild(article);});list.appendChild(section);});}catch(error){message.className='form-message error';message.textContent=error.message;}}init();
